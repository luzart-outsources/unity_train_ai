namespace TrainAI.Configs
{
    // Loai object co the tuong tac trong scene.
    public enum InteractableKind
    {
        QuestPoint = 0,  // Diem nhiem vu (san van dong, ban hoc, giuong, ...)
        NPC = 1,         // NPC chat duoc
        SceneDoor = 2,   // Cua chuyen scene
    }
}
